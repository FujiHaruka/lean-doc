# Handoff — 2026-08-11 (11)

## Relay control
- Mode: **DONE**
- Goal: 段階 7「完成品の値を出す」の残り 2 項 (**7e プレビュー構文近似** と **L3-3 増分化**)。
  **両方完遂。** `docs/approach.md` §7 の段階 7 行を **◐ → ✅** にした。
- Leg: 3 / cap 12 (**leg 3 で終端**。無進捗・leg 上限には当たっていない)
- Predecessor: `leandoc-stage7-r2` — kill 済み
- Stop-on: **completion**
- Progress ledger:
  - (r0) 前ゴール: 段階 6a〜6d + CI 軸。`a68f205`〜`9c45073`。
  - r1: 7a/7b/7c/7d/7f。byte 再現率 **63.587% → 99.506%**、意味解析 **3.23 倍**。`dcaa8b8`〜`2b9c1d5`。
  - r2: 7g (常駐の本配線 + ソーク 600 要求)、7e-rev。`a373006`、`eaf2e67`。
  - r3: **7e (構文近似) と 7h (L3-3 増分化) を完遂**。
    `0f6172d` / `690cbd1` / `b2cf9c6` / `608f6e9`。**全部 push 済み、作業ツリーは clean。**

## State

- Branch: main / clean / push 済み。`docs/approach.md` は **600 行ちょうど** —
  **次に何か足すなら先に `/compact-plan`**。
- 計測対象 `/Users/haruka/dev/lean-projects` は **clean・無傷**、doc-gen4 の計装 **APPLIED**。
- clone `/private/tmp/lean-doc-relay/clone` は **reset 済み・clean**。
- ディスク空き **17 GiB**。`w7a`〜`w7e` / `w7e-syn` / `w7g` / `w7h` (667 MB) は再現に要るので**消さない**。
- **現行の実装**: 抽出器 `experiments/stage7d/` (IR schema 4 + `--jobs`)、
  パイプライン `experiments/stage7g/` (常駐の自動起動・自動停止込み)、
  **L3-3 は `experiments/stage7h/global.ts` が現行** (stage5 版は対照として残す)、
  受け入れオラクル `experiments/stage4c/coverage.ts` (**1 行も変えない**)。
  7a/7b/7c と stage5 は凍結 (docs が数字を引用している)。

## Where we are — 段階 7 は閉じた

### 7e: 構文近似は成立するが「プレビュー」ではなく「アウトライン」

**§8 の「コストは elaboration 無しの Lean パーサ」は前提が誤っていた**【実測】 —
**Lean のパーサは環境に依存する**。`--env init` は 404/432 でエラー、`--env full`
(対象 432 を import) でも **`≐` / `H(μ; X)` / `ℝ≥0∞` は落ちる** (`scoped` が未活性)。
`--env mathlib` と `--env full` はエラー数が完全一致 = **自分のモジュールを import しても
パーサには何も足されない**。同じバイト列から出る構文木が**ノード数で 6.2 倍動く**。

**ファイル別活性化の 2 パスで 0/432 エラー**【実測】 — パス 1 で自ファイルの
`open` / `namespace` を集め、`activateScoped` してから再パース。**1 モジュール 15.4〜16.3 ms**
(= 環境ロード 2.6 秒の 0.6%)、**環境は使い回せる** (`importModules` はプロセスに 1 回)。

**取れるもの**: 宣言名 **95.7% / 偽陽性 0** (取りこぼし 204 は全部 elaboration が作るもの)。
**取れないもの**: 署名は **byte 一致 7.5%**、**37.2% の宣言は型クラス仮定が `variable` にあり
ソースに書かれていない**。**これは情報欠落であって整形差ではない。**

### 7h: L3-3 は増分化された。天井の測り方が本体だった

**増分 7 の「増分化する価値なし」は別の分母での判断で、外れていた**【実測】 —
消えるのは IR 全読み 1 回 (0.119 秒) ではなく **`build` の全読み + `delta` の全走査 +
プロセス 1 つ = 0.319 秒**。`contentHash` を鍵にした **842 KB** のキャッシュで
**step 6 が 0.4527 → 0.1257 秒 (fresh) / 0.4470 → 0.1461 秒 (resident)**、
**6 成果物もサイト 439 ファイルも byte 一致**。

**次に効くのは L3-3 ではない** — このパイプラインは IR を **7 回全読みしていた**うち
L3-3 が消したのは **2 回**で、**残る 5 回 (ownership・merge ×2・impact・render) のほうが大きい**。
**同じキャッシュの筋がそのまま使えるはず** (未着手)。

## Tasks

**空。** 段階 7 のスコープに残タスクは無い。

## 次にありうる作業 (どれも**ユーザーの優先度判断**が要る)

1. **§8 の製品判断 2 つ** — どちらも**測り終わっていて、決めるだけ**:
   - **プレビューモードを持つか** — (1) 構文アウトライン (15 ms) と (2) stale バナー
     (433 ページ中 6 = 1.39%) は**排他ではない**。重ねられる。
   - **revision をページのバイトに埋めるか** — 「単体完結を失ってよいか」。
2. **新しい最適化項目 (§8 の問いではない)** — 残る IR 全読み 5 回。7h と同じ筋。
3. **§8 で未着手のまま残っている問い** — 宣言の所有モジュール / `_private.` 名のリンク /
   Lean バージョン差の吸収 / 依存パッケージの写像配布 (「上流に同じファイルがあるか」の 1 点)。
4. **§5.6 の実装言語** — まだ決めていない。**検証段階が進む前に勝手に確定させない。**

## Files to read first

- `docs/approach.md` §7 (全段階 ✅) と §8 (未解決の問い) — 何が残っているかの SoT
- `docs/verification-log.md` の **段階 7e / 段階 7h** (この leg で足した 2 節)
- `experiments/stage7e/README.md` / `experiments/stage7h/README.md`

## Load-bearing context

- **判定オラクルは「フルビルド / doc-gen4 とのバイト一致」。** 報告を出すのは検証対象と同じ
  コードなので**報告は証拠にならない**。
- **byte 一致を保つ最も安い方法は、バイトを作るコードを触らないこと** (7h)。
  7h は「読む」ところだけを増分にし、導出コードは stage5 のまま毎回 433 モジュール回している。
- **計測の構造が結果を作る** — 7h の最初の A/B は構成の順序が固定で、
  **常に先頭の構成が直前の 3 GB 常駐サーバによるページキャッシュ退避を被っていた**
  (page fault 29,779 ↔ 2,013、3.3 秒差)。**時間だけ見ていると気づかない。**
- **`globalSeconds` の総時間への帰属はできない** (7h) — 同じペアで `extract` + `serveStart`
  だけが効果の 1.2〜2.5 倍動く。**引用してよいのは step 6 の行だけ。**
- **`coverage.ts:512` の revless 正規化は `/blob/[0-9a-f]{40}/` のハードコード。**
  `--source-url` にタグ名やブランチ名を渡す実装が出たら**採点が静かに下がる**。
- **`coverage.ts` の原因ラベルを鵜呑みにしない** / **並べ替えに盲目** (99.506% はそれ込み)。
- **`diff` はこの zsh で `colordiff` にエイリアス** (未インストール)。スクリプトでは **`/usr/bin/diff`**。
- **`git add -f` を使わない** — `.gitignore` を無効化して 163 MB を push しかけた。
- **npm/node は壊れている** (署名不正で SIGKILL)。JS が要るときは **deno**。
- **セッション間で同じ構成が動く** — 7h のセッションは 7g より重い (`serveStart` 15.0 ↔ 3.71 秒)。
  **比較は必ず同一セッションの対照を取り直す。過去の総時間と並べない。**
- **`--jobs > 1` の `ppUs` は壁時計でも CPU 時間でもない** (スレッドごと経過時間の和)。
- **採点は必ず具体的な rev を埋めた `--source-url` で** (`{{SOURCE_URL}}` 版は −3.11 pt ずれる)。
- **`serve-ctl.sh` の罠 2 つ**: `grep -c` は 0 件でも "0" を出して exit 1 / 渡すパスは全部絶対。
- **`Lean.Parser.Module.parseHeader` はモジュールごとに `mkEmptyEnvironment` を呼ぶ** —
  Mathlib ロード済みだと 1 本 0.4 ms、432 本で約 200 ms。ヘッダだけ大量に舐める設計
  (import グラフの再取得など) を考えるなら、この API を使わず自前でヘッダを読む価値がある。
- **計測ドライバは「計測」と「集計」を分ける** (`run.sh report`)。7h も同じ形にしてある。
- **subagent には「コミットするな」と指示する** — 統合とコミットはオーケストレータが持つ。

## この leg で入れた訂正

1. **「L3-3 の増分化で消せるのは 0.119 秒 = 増分実行の 1.5〜2.6%」は外れていた** —
   実際は **0.319 秒**で、`delta` の全走査とプロセス 1 つが数えられていなかった (7h)。
2. **§8 の「構文近似のコストは elaboration 無しの Lean パーサ」は前提が欠けていた** —
   パーサは環境に依存し、**常駐環境の中にいること + 対象ファイル自身の `open` を
   解いていること**が要る (7e)。
3. **ハンドオフに残っていた「L3-3 は 0.136 秒 = 増分の 2〜3%」は古い分母** —
   7g 配線での実測は **0.353〜0.372 秒 = 総時間の 4.4〜5.7%** だった。
