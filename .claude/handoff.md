# Handoff — 2026-08-10 (7)

## Relay control
- Mode: ON
- Goal: handoff (7) の「次にやるなら」1〜5 を全部完遂する
  (1 常駐を incremental.sh に配線 / 2 プレビューモード / 3 revision 非埋め込み /
   4 ubuntu-latest cold 常駐率 / 5 L3-1 の一般性)
- Leg: 1 / cap 8
- Predecessor: none
- Stop-on: completion | user-decision | no-progress×2 | leg-cap
- Progress ledger:
  - (前ラウンド, 別ゴール) handoff (6) の 1〜5 完遂 + approach.md 圧縮 → `21b2578`
  - r1: (進行中)

## State

- Branch: main / Uncommitted: clean / push 済み
- **前ハンドオフの「次にやるなら」1〜5 を全部完遂した。** 増分 4〜7 + 段階 6。
- **段階 5 の正しさが端から端で閉じた** — 本物の移動と本物の削除を複製上で `lake build`
  ごと回し、増分の出力が**フルビルドと byte 一致**するところまで実測した。
  判断点 3 (5 命題すべて否定) はこれで塞がった。
- 計測環境: 対象リポジトリ `/Users/haruka/dev/lean-projects` は **clean・無傷**。
  計装パッチは当たったまま (`--check` → APPLIED)。複製は削除済み、ディスク空き **13 GiB**。
- **抽出器のバイナリは再ビルド済み** (`--serve` が入っている)。

## Where we are

### 増分 4 — L1 の鍵を 2 つに分けた (段階 5d)

`envKey` → `extractKey` (toolchain / manifest / 抽出器 id / IR schema) と
`renderKey` (レンダラ id / `--source-url`)。振り分けは「**IR を変えうるか**」で決める。
**`--source-url` 変更が 18.39 秒 → 1.34 秒 (13.7 倍)**、分割の追加コストは +0.3 ms
(ドリフト 6.8 ms 以下)。C1〜C6 は予測どおり、**C7 だけ予測値が外れた** (25〜30 秒節約と
書いたが実際は 17.05 秒 — 27 秒は別条件の既存値だった)。

### 増分 5 — L3-1 を実装して本物の移動で検証 (段階 5e)

`ownership.ts`。**L3-1 無しは 433 ページ中 1 ページがリンク切れ、有りは 433/433 byte 一致。**
**D1 は部分否定** — 4 つの参照側のうち 3 つは L2 が拾った。**L2 が参照側を拾うかは当てにできない**
(Lean の直列化の都合であって依存構造の性質ではない)。2 ラウンドで不動点、L3-1 自体は 0.265 秒
だが 2 ラウンド目が環境ロードを丸ごと払う (4.17→7.78 秒)。

### 増分 6 — 削除経路 (段階 5f)

台帳 (作り直しで足りる) / IR (`merge-ir --remove`) / ページ (`prune-pages.ts`)。F1〜F4 成立。
**F5 は最初否定** — 削除と無関係な LZ78 のページに、行き先の消えたリンクが残った。
正体は**削除経路ではなく L3-2 (全域写像)**。増分 7 で閉じて **437/437 byte 一致**。

### 増分 7 — 全域索引と全域写像の差分 (段階 5g)

`global.ts`。**1.88 MB / 0.139 秒** (doc-gen4 の 38.6 MB は依存木全体の値でこちらの数字ではない)。
**G4 は「増分更新は書く価値がある」を偽と予測して偽で確定** — 消せるのは IR 全読み 0.119 秒だけ。
**台帳・依存スライスに続いて 3 度目の同じ答え**で、共通の原因は IR 全読みが 0.119 秒であること。
`tactics.html` だけは自パッケージの関数ではない (tactic docstring は環境側、432 モジュールで 0)。

### 段階 6 — 常駐 (段階 5h)

`Extract.lean` に `--serve`。**要求あたり 0.035〜0.088 秒** 対 単発 1.72〜4.60 秒、
**IR は単発と byte 一致**、6 要求で汚染なし。**制約: 常駐環境は再読み込みできない**ので
編集したモジュールには使えない。使えるのは L3-1 のラウンド 2 以降。
**起動 6.3 秒は単発 1 モジュール 1.7 秒より高い** → サーバが編集をまたいで生きる形でないと損。

## Tasks

なし。次に着手するなら下記から選ぶ。

## 次にやるなら (優先順)

1. **常駐を `incremental.sh` に配線する** — 段階 6 は単体で測っただけ。ラウンド 2 以降を
   サーバに投げる経路を作れば 5e の 7.99 秒が約 5.06 秒になる【外挿】。**サーバの環境が
   どの olean 世代のものかを持ち回る必要がある** (編集したモジュールに使わないため)。
2. **プレビューモード** — 段階 6 の実測で「サーバが編集をまたいで生きる形でないと元が取れない」
   と分かったので、**§8 のプレビューの問いが常駐の受け皿**になった。`lake build` が臨界パスの
   74〜98% を占めるという増分 3 の実測と合わせて、ここが端から端で効く唯一の余地。
3. **revision をページのバイトに埋めない設計** (§8) — 埋める限りコミットのたびに全 432 ページが
   無効。相対リンク + 配信時注入なら 0 ページになる。**§5.6 の出力契約の変更。** 未計測。
4. **`ubuntu-latest` での cold 常駐率** — CI 軸の床がここで確定する。ユーザー判断が要る。
   **macOS で出た否定は不利な側なので移送できない。**
5. **L3-1 の一般性** — 移動は n=1。instance / simp 補題 / `abbrev` では未確認 (instance は
   特に怪しい)。3 ラウンド以上を要する変更も作れていない (`--max-rounds` は 5)。

## Files to read first

- `experiments/stage5d/README.md` 〜 `stage5h/README.md` — 各増分の判断基準と結果
- `docs/verification-log.md` の増分 4〜7 と段階 6 — 数字の SoT
- `docs/approach.md` §5.5 — 3 層。**全層が実装済みで byte 一致まで実測されている**

## Load-bearing context

### 今回できるようになったこと

- **`incremental.sh` が本物のパイプラインになった** — detect / extract / ownership / merge /
  prune / global / render の 7 段、L3-1 はラウンドループ、`--l3-1 on|off` で対照が取れる。
- **判定オラクルは「フルビルドとのバイト一致」**。報告を出すのは検証対象と同じコードなので、
  報告は証拠にならない。5e/5f はこれで 2 度救われた。
- 抽出器の `--serve` は stdin に `<modules.txt> <out.jsonl> [<ir-dir>]` を 1 行ずつ。
  返事は `ok <code> <ns>`。**`run` の人間向けレポートも stdout に出る**ので、
  プロトコル行は接頭辞で拾うこと。

### 計測の落とし穴 (今回効いたもの)

- **複製上の baseline はパス埋め込みで汚れる。** `cp -Rc` はビルド木ごと複製するので olean は
  元のパスで作られており、複製内で最初に再ビルドされたモジュールは**パス長の差だけで変わる**
  (実測 +80 B)。**気づけた決め手は「編集を戻してもう一度 check した」こと** — 移動の結果なら
  移動を取り消せば消えるはずが、消えなかった。→ `stage5e/rebuild-own.sh` (431 モジュール、
  624.9 秒 wall / 2,899 秒 CPU)。**複製上の baseline は複製内で再ビルドしてから取る。**
- **題材選びを 1 回間違えた。** 段階 5c の C は**宣言 0 個のドキュメント専用モジュール**で、
  参照は docstring 内のバッククォート。**A の宣言を印字署名で参照するモジュールは 0 個**
  だった。olean を見る 5c では問題なかったが、IR の `refs` を問う 5e では観測不能な題材。
  → 題材は IR の census から選ぶ (`stage5e/run.sh` は参照側 0 なら走らない)。
- **「0 ページ」は 2 通りの間違いで出る。** 全域写像を自宣言だけで作った / 走査が code span を
  丸ごと照合した。どちらも外からは同じに見え、バイト比較が無ければ見えなかった。

### 前ハンドオフから引き継ぎ、まだ生きているもの

- **`ubuntu-latest` での cold 常駐率** (上の 4)。
- `benchmarks/tools/olean-{evict,residency,prefetch}.c` — バイナリは .gitignore 済みなので
  `cc -O2` で再ビルドが要る。**macOS の `MADV_WILLNEED` は同期、Linux は非同期。**
- IR は scratch にしか無い。**消えても `experiments/stage5/extract-once.sh` で 27 秒**
  (`--modules` は絶対パスで渡すこと — スクリプトが対象リポジトリへ `cd` するので相対は壊れる)。
