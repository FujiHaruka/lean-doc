# Handoff — 2026-08-11 (10)

## Relay control
- Mode: **ON**
- **予算制約は解除された** — ユーザーが「週次上限に当たって止まっても構わない、進めて」と明示。
  **`usage-budget` は撤退理由として使わない。**残り 2 項を完遂まで走る。
- Goal: **段階 7「完成品の値を出す」の残り**。残っているのは **2 項だけ**:
  - **L3-3 全域成果物の増分化** — 現状「毎回作り直す」で 0.136 秒 = 増分の 2〜3%。
    **§8 上は既に決着済みの行**なので、これは最適化であって未解決の問いではない。**優先度は低い。**
  - **7e: プレビュー構文近似のコスト計測** — §8「プレビューモードを持つか」の選択肢 (1)。
    **§8 に残る唯一の未計測の分岐**で、こちらが本命。**決定はユーザーに残す (測るだけ)。**
- **着手順**: **7e (プレビュー構文近似) が先** — §8 に残る唯一の未計測の分岐。L3-3 はその後。
- Leg: 3 / cap 12
- Predecessor: `leandoc-stage7-r2` — **走り出しを確認したら `tmux kill-session -t leandoc-stage7-r2`**
- Stop-on: completion | user-decision | no-progress×2 | leg-cap
  (**usage-budget は外した** — ユーザーが上限に当たってよいと明示した)
- Progress ledger:
  - (r0) 前ゴール: 段階 6a〜6d + CI 軸。`a68f205`〜`9c45073`。
  - r1: **7a/7b/7c/7d/7f を完遂。** byte 再現率 **63.587% → 99.506% (到達上限)**、
    完全一致ページ **34 → 304/348**。意味解析を並列化して **3.23 倍 (IR は byte 一致)**。
    CI 軸は Linux で `importModules` を実測し **◐ → ✅**。`dcaa8b8`〜`2b9c1d5`。
  - r2: **7g (常駐の本配線 + ソーク 600 要求) と 7e-rev (rev の未検証 2 点) を完遂。**
    `a373006`、`eaf2e67`。**全部 push 済み、作業ツリーは clean。**
    予算撤退ラインで一度 PAUSED にしたが、**ユーザーが「上限に当たって止まっても構わない、
    進めて」と明示したので解除し、新規セッション (leg 3) へ継投**した。

## State

- Branch: main / clean / push 済み。`docs/approach.md` は **600 行ちょうど** — **次に何か足すなら
  先に `/compact-plan`**。
- 計測対象 `/Users/haruka/dev/lean-projects` は **clean・無傷**、doc-gen4 の計装 APPLIED。
- clone `/private/tmp/lean-doc-relay/clone` は **移動前に reset 済み・clean**。
- ディスク空き **12 GiB**。`w7a`〜`w7d` / `w7e` (87 MB) / `w7g` (115 MB) は再現に要るので**消さない**。
- **現行の実装**: 抽出器 `experiments/stage7d/` (IR schema 4 + `--jobs`)、
  パイプライン `experiments/stage7g/` (常駐の自動起動・自動停止を含む本配線)、
  受け入れオラクル `experiments/stage4c/coverage.ts` (**1 行も変えない**)。
  7a/7b/7c は凍結 (docs が数字を引用している)。

## Where we are

### 常駐は本配線まで閉じた (7g)

**現行の抽出器で配線し直した数字**【実測】: 自動起動・自動停止込みで **439/439 byte 一致**のまま
増分 2 ラウンドが **7.968 → 6.668 秒 (−16.3%, `--jobs 1`) / 7.894 → 6.509 秒 (−17.5%, `--jobs 4`)**。
ソークは **600/600 成功、IR は #1/#300/#600 で byte 一致、RSS 0.55 GB で +0.00%**。

**設計上の含意**: 常駐が消すのは `analyze` ではなく**ラウンドごとの `importModules`
(1.819 + 1.766 秒) とプロセス起動 2 回分**で、**どちらも並列化されない**。だから増分では
`--jobs` が取り分を動かさず、フル再抽出では分母だけ縮んで **−23.4% (j1) → −43.3% (j4)** になる。
**§5.1 の 2 本の柱 (1 環境 / 長寿命) は独立ではなく、互いの取り分を増やす。**

### rev 非埋め込みは「未検証」が 1 つに減った (7e-rev)

**オラクルは注入後なら成立する**【実測】: 具体 rev の木と注入後の木は `/usr/bin/diff -r` で
**全木 byte 一致**、採点も **99.5062% / 完全一致 304 ページ**で同一。プレースホルダのまま採点した
版だけが **−3.1103 pt** (gh_link / nav_gh が**きっかり 0.0%**)。
**順序制約は注入方式で変わる** — 配信時置換版には**制約が無い**【実測】。
**残る未検証は JS 注入版の順序だけ** (ブラウザが要る)。

### 7e の形 (leg 2 からの申し送り。**測る前に検証すべき前提が 1 つある**)

§8 は構文近似のコストを「elaboration 無しの Lean パーサ」と書いているが、**Lean のパーサは
環境に依存する** — `scoped notation` / `macro` / カスタム構文はモジュールが宣言するもので、
このプロジェクトは実際に `H(μ; X)` や `≐` を使う (段階 2 で doc-gen4 がそれを出せていないことを実測)。
**したがって「パーサだけ」でも import が要る可能性が高い。**これが真なら構文近似の「安さ」は
常駐環境の中にいることが前提になり、**段階 6d が否定した「常駐が受け皿」と同じ土俵に戻る。**
まずここを実測してから時間を測ること。

測るべきは最低 4 つ:

1. **コスト** — 常駐環境 (432 モジュール import 済み) の中で 1 モジュールの `.lean` を構文木に
   するまで。中央値と分布。432 モジュール全部でも取る。**プレビューの実用単位は「編集中の 1
   モジュール」**なので、その 1 本が測るべき数字。
2. **環境依存** — 空環境 / Mathlib のみでパースしたときの失敗・取りこぼし。上の前提の検証。
3. **見える宣言の差** — 構文から取れる宣言名 vs 環境から取れる 5,123 宣言。
   **equation lemma・instance・structure projection は構文に現れない**ので大きく落ちるはず。
   落ち幅が大きいなら、それは「プレビュー」ではなく**アウトライン**であり、製品判断の入力が変わる。
4. **署名の精度** — 構文の生テキスト vs IR の pretty-print 済み署名の byte 一致率。
   `coverage.ts` の枠組みが使えなければ素朴な署名領域の一致率でよい (**`coverage.ts` は変えない**)。

撤退ライン: 1 が秒オーダーなら構文近似の利点が消え、§8 の選択肢は (2) だけになる。
**決定はユーザーに残す — 測るだけ。**

## Tasks

Task list は空から作り直してよい。残りは Goal の 2 つだけ (**7e が先**)。

## Files to read first

- `docs/approach.md` §8 の「**プレビューモードを持つか**」の行 — 7e の入力そのもの
- `docs/verification-log.md` の **段階 6d** (「常駐が受け皿」を否定した節) と **段階 7g / 7e-rev**
- `experiments/stage7g/README.md` — 現行のパイプライン配線
- `experiments/stage7d/README.md` — 現行の抽出器と `--jobs`

## Load-bearing context

- **判定オラクルは「フルビルド / doc-gen4 とのバイト一致」。** 報告を出すのは検証対象と同じ
  コードなので**報告は証拠にならない**。この 2 晩で何度もこれに救われた。
- **`coverage.ts:512` の revless 正規化は `/blob/[0-9a-f]{40}/` のハードコード。**
  `--source-url` にタグ名やブランチ名を渡す実装が出たら**採点が静かに下がる** (7e-rev)。
- **`coverage.ts` の原因ラベルを鵜呑みにしない** — 残った領域は中身を見て確認する (7b の教訓)。
- **`coverage.ts` は領域を宣言名で対応付けるので並べ替えに盲目**。99.506% はそれを含んだ数字。
- **`diff` はこの zsh で `colordiff` にエイリアスされている** (未インストール)。
  スクリプトでは **`/usr/bin/diff`**。exit 127 を「差あり」と誤読する。
- **`git add -f` を使わない** — `.gitignore` を無効化して 163 MB を push しかけた。
- **npm/node は壊れている** (署名不正で SIGKILL)。JS が要るときは **deno**。
- **セッション間で同じ構成が 0.34 秒動く。** 比較は必ず**同一セッションの対照**を取り直す。
- **抽出器やレンダラが変わったら過去の数字と並べない。** 7g で 6a の 7.940 / 6.168 秒を
  捨てたのがその例 (schema 3 → 4)。**7g の `fresh-j1` 7.968 秒が今の基準。**
- **`--jobs > 1` の `ppUs` は壁時計でも CPU 時間でもない** (スレッドごと経過時間の和)。
- **採点は必ず具体的な rev を埋めた `--source-url` で描いたページに対して**
  (`…/blob/573793b243fb1343636088eb62d1789ab2b14cec`)。`{{SOURCE_URL}}` 版は −3.11 pt ずれる。
- **`serve-ctl.sh` の罠 2 つ**: `grep -c` は 0 件でも "0" を出して exit 1 / サーバは対象リポジトリへ
  `cd` するので**渡すパスは全部絶対**。相対だと静かに死んで 600 秒待つ。
- **subagent には「コミットするな」と指示する** — 統合とコミットはオーケストレータが持つ。
- **計測ドライバは「計測」と「集計」を分ける** (7g の `run.sh report`)。25 分の計測に対し集計は
  1 秒なので、要約の書き直しが再計測を要求しなくなり、**生成物を手で直す誘惑が消える**。
  既存の段階のドライバは一体なので、同じ分離を入れると数字の改竄リスクが構造的に下がる。

## この leg で入れた訂正

1. **常駐 1 台の待機時 RSS は 0.55〜0.62 GB。** 既存 docs の 3.27〜3.37 GB は**要求処理中のピーク**で、
   「常駐が常時 3.3 GB を占める」と読める書き方は不正確だった (7g)。
2. **「注入が先に走らないといけない」は 2 つの注入方式を同じ括弧に入れていた** — 配信時置換版には
   制約が無く、代償リストの中で唯一「実測で消える」項目だった (7e-rev)。
3. **「リンク 4,993 本」「注入 0.1893 秒」はどちらも木の規模に依存する数字なのに規模が無かった** —
   規模を添えた (7e-rev)。
