# Handoff — 2026-08-10 (6)

## Relay control
- Mode: ON
- Goal: handoff の「次にやるなら」1〜5 を全部完遂する
  (1: L1 の鍵を 2 種類に分ける / 2: L3-1 の実装 / 3: 削除経路 /
   4: 全域索引の増分更新 / 5: 段階 6 常駐)
- Leg: 1 / cap 8
- Predecessor: none
- Stop-on: completion | user-decision | no-progress×2 | leg-cap
- Progress ledger:
  - (まだ無し)

## State

- Branch: main / Uncommitted: clean / push 済み
- **段階 5c を完遂した。** 前ハンドオフの「次にやるなら」の 1 (参照側 olean) と 4 (ledger の穴)
  が終わり、`compare-pages.py` の既定値バグも塞いだ。`approach.md` は 600 行ちょうど。
- 計測環境: 対象リポジトリ `/Users/haruka/dev/lean-projects` は **clean・無傷**。計装パッチは
  当たったまま (`benchmarks/tools/apply-instrumentation.sh --check` → APPLIED)。
  `.lake/build/doc` の 348 ページは**正解データなので読むだけ**。
- ディスク空き **14 GiB**。実験に使った複製 (lp-clone) は削除済み。

## Where we are

### 段階 5c =【参照側 olean は宣言の移動を検出できるか】→ **できない (実測)**

**制約を外す方法が見つかった** — 「対象リポジトリを書き換えない」を守ったまま `lake build` を
回すには、**APFS の clonefile (`cp -Rc`) で複製を作ればいい**。COW なので 12 GB の複製で
**実消費ディスクは 0**、原本は 1 byte も変わらない。34 秒でできる。**この手が使えることが
今回いちばん再利用が効く発見** — 増分 2 が「対象の複製が要る」で止めた項目は全部これで測れる。

判断基準は**計測前に commit** (`04a5b2d`)。結果:

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| P1 | olean の生成は決定的 | 真 | **真** (パスを固定すれば。再ビルド 4 回とも byte 一致) |
| P2 | 未参照の宣言を A に足しても C の olean は不変 | 真 | **真** (再ビルドはされるが byte 一致) |
| P3 | 宣言が A→X へ移動すると参照側 C の olean が変わる | 偽 | **偽** (1 byte も変わらない) |

→ **L3-1 (名前の所有権) は必須で確定。** 同時に P2 が真なので**再抽出集合を import 閉包へ
広げる必要は無い**ことも確定した (広げるべきは出力側だけ)。

### 副次的に出た 3 つ (どれも設計に効く)

1. **`lake build` が増分の臨界パスの 74〜98%**【実測】。1 モジュール変更で warm
   **12.40〜12.73s** (直接 importer 2 = best case に近い)、fan-in が大きいと **237〜243s**
   (127 モジュール再ビルド)。doc 側の増分は 3.30〜4.39s。
   **doc 側をゼロにしても 12.4 秒より速くならない。**
   → `approach.md` §5.1 で**常駐 (段階 6) の優先度を下げ**、§8 で**プレビューモードの重みを
   上げた**。前ハンドオフの優先順 2 (段階 6) はこれで根拠が弱くなっている。
2. **olean にソースの絶対パスが 429/432 モジュールで埋まっている**【実測】。正体は
   Mathlib style linter のログを持つ環境拡張 `lintLogExt` で、警告文にパスが入ったまま
   保存されている (警告が出ないモジュール 3 つだけがパスを持たない → 429 と 429 が一致)。
   **olean の内容ハッシュがチェックアウト先に依存する**ので、CI と開発機で IR キャッシュを
   共有すると**ヒット 0**。124 モジュール規模でも再確認済み (サイズ差が +72/+80 の 2 値のみ)。
3. **Lake は孤児 olean を消さない**【実測】。現行 432 モジュールに対し `.lake` の olean は
   1,090 個で **659 個が幽霊**。§5.5 の「モジュール一覧をディスク走査で作る」案は、
   走査先を `.lake/build` にすると幽霊を拾う。**列挙元はソースの glob。**

### ledger の穴 3 つを塞ぎ、前後を同一セッションで測り直した

L1 を変更集合に効かせる / `--ir` を渡す / モジュール一覧の凍結を外す。
**before → after → before** を連続実行 (2 つ目の before はドリフト検査)。
**detect は +1〜2 ms でドリフトと同じ大きさ、total は 3 ブロックで区別できない。無料だった。**
出力も 3 ブロックで一致。→ `benchmarks/results/stage5c-ledger-fix-summary.txt`

**増分 1 の 3.30〜4.39 秒は置き換えていない** — 今回のセッションは 4〜6% 速いが、それは
セッション差 (この間に複製上で 127 モジュールのビルドを何度も回した)。**再現性の確認**であって
置き換えではない。

## Tasks

なし。次に着手するなら下記から選ぶ。

## 次にやるなら (優先順)

1. **L1 の鍵を 2 種類に分ける** — 今の実装は鍵が変われば 432 モジュール全部を再抽出する。
   `--source-url` の変更は**再レンダリングだけで足りる**はず。**穴を塞いだ直後なので、
   ここが一番自然な続き** (→ `approach.md` §5.5 L1)。
2. **L3-1 を実装する** — 必須と確定した層。「そのモジュールが定義していた名前の集合の差分」を
   L2 の核から取り、その名前を `refs` に持つモジュールを再抽出集合に足す。
   **複製があれば本物の移動リファクタで検証できる** (段階 5c の E-B がそのまま雛形)。
3. **削除経路** — `incremental.sh` は削除を検出したら exit 4 で止まるようにしただけ。
   台帳・IR・ページの 3 箇所から消す経路が要る。
4. **全域索引 (38.6 MB / 5 ファイル) の増分更新** — 規模は実測、時間は未測定。
   ただし**端から端では 1〜3% にしかならない**ので、速度目的なら優先度は低い。
   やる理由は速度ではなく「未実装だから」。
5. **段階 6 (常駐)** — 消える 3.1 秒は端から端の 20% 未満、fan-in が大きければ 1% 台。
   **今回の実測で優先度が下がった。**

## Files to read first

- `experiments/stage5c/README.md` — 判断基準 P1〜P3 の宣言と結果、複製の作り方
- `benchmarks/results/stage5c-olean-propagation-summary.txt` — 段階 5c の全数字
- `benchmarks/results/stage5c-ledger-fix-summary.txt` — 修正前後の再計測
- `docs/approach.md` §5.5 — 3 層。**L3-1 だけが実測で裏付けられ、L1/L2/L3-2/L3-3 は未検証**

## Load-bearing context

### 今回できるようになったこと

- **対象リポジトリの複製が 34 秒・ディスク 0 で作れる** (`cp -Rc`)。
  複製内で `lake build` を回してよい。**原本は触らない**という制約はそのまま守れる。
  複製の健全性は `lake build --no-build` が `All targets up-to-date` を返すことで確認する。
- `experiments/stage5c/` に一式ある: `import-graph.py` (パッケージ内 import グラフ) /
  `snapshot-oleans.py` (olean 全数の sha256 + lake_hash を記録・比較) /
  `measure-lake-build.sh` / `run-fanin.sh` / `compare-ledger-fix.sh` (before/after/before)。
- **`compare-ledger-fix.sh` は `experiments/stage5/` の外に置いてある** — `git checkout` で
  そのディレクトリを差し替えながら走るので、自分がその中にいると壊れる。

### 計測の落とし穴 (今回また効いたもの)

- **1 回だけ測っていたら 26.89 秒を「1 モジュール変更のコスト」として記録していた。**
  実際は系列を繰り返すと 12.40 秒に収束する (major fault 167,377 → 29,176)。
  **壁時計は 2.2 倍動くが CPU 時間は安定**していたので、そこで気づけた。
- **snapshot ツールが olean を全数読むと次の計測が汚れる** (直後の `lake build` が
  25.06 秒に跳ねた)。**重い読み出しの直後に計測しない。**
- **「有利な数字」が出たら疑う、が今回も効いた** — 最初 P2 が否定に見えた (C の olean が
  +80 B) が、正体は複製のパス長差だった。**S1 と S2 を比べて初めて分かった。**

### 前ハンドオフから引き継ぎ、まだ生きているもの

- **`ubuntu-latest` での cold 常駐率** — CI 軸の床がここで確定する。ユーザー判断が要る
  (Linux 実機 / CI の実行)。**macOS で出た否定は不利な側なので移送できない。**
- **autolink の逆索引を持つか、増分では閉じないと明示するか** (未解決トークン
  8,965 distinct / 117,323 出現)。
- `benchmarks/tools/olean-{evict,residency,prefetch}.c` — バイナリは .gitignore 済みなので
  `cc -O2` で再ビルドが要る。**macOS の `MADV_WILLNEED` は同期、Linux は非同期。**
- IR (schema 2 / 432 モジュール / 16 MB) は scratch にしか無い。
  **消えても `experiments/stage5/extract-once.sh` で 27 秒** (`--modules` は絶対パスで渡すこと —
  スクリプトが対象リポジトリへ `cd` するので相対パスは壊れる)。
