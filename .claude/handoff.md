# Handoff — 2026-08-10 (8)

## Relay control
- Mode: PAUSED
- Goal: handoff (7) の「次にやるなら」1〜5 を全部完遂する
- Leg: 1 / cap 8
- Predecessor: none
- Stop-on: user-decision
- Progress ledger:
  - r1: **1・2・3・5 を完遂** (段階 6a / 6d / 6b / 6c を新設、全部 byte 一致オラクルで判定)。
    approach.md を 634→605 行に圧縮。`a68f205`〜`1975eae`、全部 push 済み。
    **4 だけ残** — 計測用 workflow は書いて push したが**トリガーはユーザー判断**。
- 聞きたいこと: **`ubuntu-latest` の計測 workflow を回してよいか** (下記「ユーザー判断が要る 1 点」)

## State

- Branch: main / Uncommitted: clean / push 済み
- 計測対象 `/Users/haruka/dev/lean-projects` は **clean・無傷**。計装は APPLIED。
- **複製は残してある**: `/private/tmp/lean-doc-relay/clone`
  (自パッケージ 431 モジュールを複製内で再ビルド済み = baseline が取れる状態)。
  **ただし `…BroadcastChannel/Basic` の移動が当たったまま。**
  次に使うときは `experiments/stage5e/setup-clone.sh reset` から
  (Basic は参照側 33 モジュールなので reset のビルドが 5 分ほどかかる)。
- 残した作業ディレクトリ: `w6a` (段階 6d の入力なので消すと 6d が再現できない) / `w6c`。
  ディスク空き **13 GiB**。
- 抽出器のバイナリは再ビルド不要 (`--serve` 入り、`experiments/stage4b/build/extract`)。

## Where we are

### 段階 6a — 常駐をパイプラインに配線した (item 1)

**段階 6 の「ラウンド 2 以降なら常駐で安全」は誤りだった。** モジュールの IR は自分の olean の
関数ではなく、参照の所有者を**環境**から引く (`Extract.lean:1352`)。その所有者列を直すことが
L3-1 がラウンド 2 を回す唯一の理由なので、**編集前環境はまさにその古い組を返す** —
実測で参照側 IR が 10,263 B 対 正解 10,267 B、**439 ページ中 1 ページが誤り**。

| 変種 | 常駐が担う範囲 | 中央値 | 正しさ |
|---|---|---:|---|
| `fresh` | なし (現行) | 7.850 | 439/439 |
| `pre` | ラウンド 2、**編集前**環境 | 5.284 | **1 ページ誤り** |
| `post` | ラウンド 2、ビルド後環境 | 5.302 (+起動 3.486 = **8.788 = 損**) | 439/439 |
| `postall` | **全ラウンド**、ビルド後環境 | 2.682 (+起動 = **6.168 = 得**) | 439/439 |

- **W0 が意外な向きで成立**: サーバは `lake build` を生き延び、その後も 0.151 秒で応答した。
  **死なずに誤答する**のがいちばん厄介な失敗の仕方。
- **`--serve-from` の既定を 2 → 1 に変えた。** 安全性はラウンド番号ではなく
  **サーバの olean 世代**が決める。
- 5.06 秒【外挿】は**到達不能**。正しくて速い数字は **6.168 秒 (−22%)**。

### 段階 6d — プレビューは常駐の受け皿にならない (item 2)

**U1 の集合は空**: P が答えた 1 モジュールは前回のビルドと byte 一致かつ誤り。
編集前環境は編集前 olean の関数なので、**正しく出せるページは既にディスクにあるものと同一**。
→ §8 のプレビューの選択肢が **3 つ → 2 つ**に減った (常駐が落ちた)。
「前回のページ + 未更新表示」には数字が付いた: **stale は 433 ページ中 6 = 1.39%**、
うち **5 (83%) はビルド前に台帳だけで名指せる**。残る 1 は L3-1 の参照側で**原理的に事前不可**。

### 段階 6b — revision をバイトから外す (item 3)

**実装コストはゼロだった** — `--source-url` は前置文字列なので `{{SOURCE_URL}}` を渡すだけで
renderKey からも rev が外れる。取り分【全部実測】: **再生成 433 → 0 ページ**、
**合計 1.6227 → 0.6441 秒 (−60.3%)**、レンダリング段 **0.9017 → 0.0256 秒**、
ページ **−399,440 B**、逆置換で **439 ファイル byte 一致**、置換注入は **0.1893 秒**。

- **V0 は潜在バグだった** — `render.ts` は `--only` 無しで全モジュールを描くので、
  空の再生成集合が **433 ページ全部**を意味していた。修正済み (集合が空ならレンダラを呼ばない)。
  **rev がバイトにある限り誰もこの経路に来なかった。**
- **B の残り 0.64 秒はほぼ全域成果物** (L3-3)。増分 7 の「増分化する価値なし」判断は
  この契約では**費用の全部**になるので、継承ではなく再検討に値する。

### 段階 6c — L3-1 の一般性 (item 5)

**効く範囲は「移動する名前が印字署名に出るか」で決まる。**

| 種別 | L3-1 on | L3-1 off | 結論 |
|---|---|---|---|
| `instance` | 439/439 | **439/439** | **L3-1 は無関係**。救っているのは L3-3 |
| `abbrev` (弱い witness) | 439/439 | 3 ページ誤り | **必須** |
| `abbrev` (参照側 33) | 439/439 (stale **30**) | **30 ページ誤り** | **必須**、5e の 30 倍の範囲でなお完全一致 |
| `@[simp]` | — | — | **この対象では検証不能** |

- **census が対象全体で言っていること**: 署名に名前が出る instance は **1 つも無い**。
  `@[simp]` も 70 宣言中 **0**。どちらも「証明側の事実 / 型ではない」から。
- **instance を救っているのは L3-3** — instance 一覧はページのバイトに無く
  `declaration-data.bmp` からブラウザが埋め、そのファイルは毎回作り直される。
  **3 層でいちばん安い層が最も広い網だった。**
- **2 ラウンドは構造的な上限** (ラウンド 2 に 30 モジュール入っても `lost 0 / gained 0`)。
  **穴は 1 つ**: 宣言集合は `constNames` を `isBlackListed` で濾したもので、述語が環境を参照する。
  反転させる変更があればラウンド 3 が出るので `--max-rounds` は残す。

## Tasks

なし。**ユーザー判断が 1 点だけ残っている。**

## ユーザー判断が要る 1 点 (item 4)

**`ubuntu-latest` で `lake exe cache get` 直後の olean 常駐率を測る workflow を回してよいか。**

- 用意したもの: `.github/workflows/ci-olean-residency.yml` (**push 済み・`workflow_dispatch` のみ**
  なので勝手には走らない)。lean-doc (private) 側で回し、対象は public な
  `FujiHaruka/information-theory` を checkout する。**計測対象リポジトリには何も書かない。**
- なぜローカルで代替できないか: macOS で出た否定は**不利な側**なので移送できず、
  かつ決め手は「ランナーの RAM で 5〜6 GB の olean が page cache に残るか」なので
  ローカルの Linux VM では別の問いになる。
- 費用: private repo の Actions 分を消費する。**60 分 timeout**、実際は `cache get` +
  mincore スナップショットで 10〜20 分の見込み。
- 回し方: `gh workflow run ci-olean-residency.yml -R FujiHaruka/lean-doc`
- ここが埋まるまで **CI 軸の床は確定しない** (§3・§8 の未着手項目)。

## 次にやるなら (item 4 以外)

1. **常駐を「1 パイプライン 1 台」で本配線する** — 段階 6a は `--serve-dir` を手で渡す形。
   自動で立てて畳む形にすると 2 ラウンド以上の増分が常に −22%。**起動を run の中で払うこと、
   ラウンド 1 も任せること**が条件 (`--serve-from 1`)。
2. **全域成果物 (L3-3) の再検討** — 段階 6b の契約では rev-only コミットの費用の全部が
   ここ (0.64 秒中のほぼ全部)。増分 7 の「不要」判断は別の分母での判断だった。
3. **rev 非埋め込みの残り 2 点** — `jump-src.js` の実行時 `href` 読みと `coverage.ts` の
   受け入れオラクル。どちらも**未検証**で、どちらもブラウザ / 比較基盤の側。
4. **常駐のソークテスト** — 6 要求は 600 要求について何も言わない (§8 に残した唯一の項)。
5. **プレビューの二択を決める** — 構文近似 (未計測) 対 未更新表示 (1.39% / 83% 事前判明)。
   **(2) には数字があり (1) には無い**ので、次は (1) のコストを測るか (2) を実装するか。

## Files to read first

- `experiments/stage6a/README.md` 〜 `stage6d/README.md` — 各段階の判断基準と結果
- `docs/verification-log.md` の段階 6a〜6d — 数字の SoT
- `docs/approach.md` §5.1 (常駐の寿命) / §5.5 L3-1・L3-3 / §8 (revision・プレビュー)

## Load-bearing context

- **判定オラクルは「フルビルドとのバイト一致」。** 報告を出すのは検証対象と同じコードなので
  報告は証拠にならない。今回も 6a (pre の誤り) と 6b (V0 の潜在バグ) の 2 度これで救われた。
- **`serve-ctl.sh` の罠 2 つ**: (a) `grep -c` は 0 件でも "0" を出して exit 1 するので
  `|| echo 0` は "0\n0" になる、(b) サーバは対象リポジトリへ `cd` するので
  **渡すパスは全部絶対**。相対だと静かに死んでこちらは 600 秒待つ。
- **`isBlackListed` は環境を参照する** — 2 ラウンド上限の唯一の穴。
- **数字の訂正を 1 件入れた**: 「0.87 秒の大半は IR 全読みの固定費」は**誤り**
  (固定費 18% / ページ比例 82%)。固定費が大半なのは 1 ページ再生成のケース。
  この誤りは §8 の「rev を埋めない」判断を過小評価させていた。
- **4,992 と 4,993 はどちらも正しい** — 前者は 432 モジュール、後者は移動後の 433 モジュール。
