# Handoff — 2026-08-11 (9)

## Relay control
- Mode: ON
- Goal: **段階 7「完成品の値を出す」の残り**。7a〜7c (byte 再現率) / 7d (意味解析) / 7f (CI 軸) は
  **完遂済み**。残っているのは:
  - **7d-系 (前 handoff の 1〜4)**: 常駐を「1 パイプライン 1 台」で**自動起動・自動停止に本配線** /
    **L3-3 全域成果物の増分化** / **rev 非埋め込みの残り 2 点** (`jump-src.js` の実行時 `href` 読み、
    `coverage.ts` の受け入れオラクル) / **常駐のソークテスト** (600 要求)
  - **7e**: プレビュー **(1) 構文近似のコスト計測**。**決定はユーザーに残す** (測るだけ)
  - **新しく出た項**: **常駐と先読みの取り分を `--jobs 4` 前提で測り直す** — 並列化で
    `importModules` が合計の 38.8% = 最大項に変わったので、両者の相対的な重みが倍近くなった
- Leg: 2 / cap 12
- Predecessor: none (leg 1 はユーザーの元セッションで tmux 名を持たない。**kill しない**)
- Stop-on: completion | user-decision | no-progress×2 | leg-cap
- Progress ledger:
  - (r0) 前ゴール: 段階 6a〜6d + CI 軸。`a68f205`〜`9c45073`。
  - r1: **7a/7b/7c/7d/7f を完遂。** byte 再現率 **63.587% → 99.506% (到達上限)**、
    完全一致ページ **34 → 304/348**。意味解析を割って**並列化で 3.23 倍 (IR は byte 一致)**。
    CI 軸は Linux で `importModules` を実測し **◐ → ✅**。訂正 2 件 (先読みの結論・cold のレンジ)。
    `dcaa8b8`〜`b19843d`、全部 push 済み。

## State

- Branch: main / clean / push 済み。**`docs/approach.md` の圧縮 (606 → 600 行) が
  着地しているか確認すること** (未コミットなら内容を見てからコミット)。
- 計測対象 `/Users/haruka/dev/lean-projects` は **clean・無傷**、計装 APPLIED。
- ディスク空き **13 GiB**。作業ディレクトリ `/private/tmp/lean-doc-relay/w7a`〜`w7d` は
  それぞれの段階の再現に要るので**消さない**。`clone` は
  `…BroadcastChannel/Basic` の移動が当たったままなので `experiments/stage5e/setup-clone.sh reset` から。
- **現行の実装は `experiments/stage7d/`** (IR schema 4 + `--jobs`)。7a/7b/7c は**凍結**
  (docs が数字を引用しているので触らない)。抽出器のビルドは `experiments/stage7d/build.sh`。

## Where we are

### byte 再現率は到達上限に達した (7a → 7c)

| | |
|---|---|
| 63.587% → **99.506%** | 残る 0.494% は **rev のみ = オラクル側の天井** (doc-gen4 の参照ツリー自身が 2 revision を持つ) |
| 完全一致ページ **34 → 304 / 348** | 生成側の総バイトが doc-gen4 と**同数** (22,028,728) |

**「出力範囲の差」という留保が §6 から消えた。** 内訳: 7a が `splitWhitespaces` (32.9 pt、
IR schema 3 で front/back 幅を持たせた)、7b が属性・instance 索引・メンバ (0.63 pt)、
7c が autolink 索引 (1.862 pt) と CommonMark (0.546 pt)。

### 意味解析は並列化で 3.23 倍 (7d)

`analyze` の内訳は **delaborate 55.3% / format 15.4% / タグ付け 12.7% / parenthesize 8.7%**。
**分布はほぼ一様** (上位 1% で 6.97%) なので外れ値を潰す筋は無い。
`--jobs 4` で **10.79 → 3.34 秒**、合計 **14.02 → 6.50 秒**、**IR は byte 一致**。
**減ったのは壁時計で CPU 時間ではない** (instructions +1.0%)。頭打ちは 4 スレッド。

### CI 軸は閉じた (7f) — ただし訂正が 2 件

**床は配置で決まる**: doc 生成を `lake build` と同じジョブに置けば `importModules` **2.61 秒**
(major fault 26)、別ジョブなら **20〜89 秒**。**先読みはランナー実体で答えが割れる**
(遅い実体で壁時計 −23〜−44%、速い実体で −4%)。

## Tasks

Task list は空の状態から作り直してよい。残りは上の Goal の 3 つ (7d-系 / 7e / 並列前提の測り直し)。

## Files to read first

- `docs/approach.md` §5.1 (常駐) / §5.5 L3-3 / §8 (revision・プレビュー) — 計画の SoT
- `docs/verification-log.md` の**段階 7 準備 / 7a / 7b / 7c / 7d** と **CI 軸の 3 節** — 数字の SoT
- `experiments/stage7d/README.md` — 現行の実装と `--jobs`
- `experiments/stage6a/README.md` — 常駐の配線 (7d-系の入力)

## Load-bearing context

- **判定オラクルは「フルビルド / doc-gen4 とのバイト一致」。** 報告を出すのは検証対象と同じ
  コードなので報告は証拠にならない。この 1 晩で 4 回これに救われた。
- **`coverage.ts` の原因ラベルを鵜呑みにしない** — members の不一致を全部「メンバのデータ欠落」
  に入れる規則だが、実際は autolink だった (段階 7b)。**残った領域は中身を見て確認する。**
- **`coverage.ts` は領域を宣言名で対応付けるので並べ替えに盲目** — rev を伏せても一致しない
  ページが 2 枚ある (doc-gen4 の不安定 qsort)。99.506% はそれを含んだ数字。
- **`diff` はこの zsh で `colordiff` にエイリアスされている** (未インストール)。
  スクリプトで `diff` を使うと **exit 127 を「差あり」と誤読する**。**`/usr/bin/diff` を使う。**
- **`git add -f` を使わない** — `.gitignore` を無効化して 163 MB のビルド成果物を push しかけた。
- **セッション間で同じ構成が 0.34 秒動く。** 比較は必ず**同一セッションの対照**を取り直す。
- **CI のランナー実体は cold 性能が 3.3 倍違う** (fault 11,949 の遅い実体 / 30,424 の速い実体)。
  **run をまたいだ CI 数字の比較は成立しない。** 変種は同一 run 内に置く。
- **`--jobs > 1` の `ppUs` は壁時計でも CPU 時間でもない** (スレッドごと経過時間の和)。
- **採点は必ず具体的な rev を埋めた `--source-url` で描いたページに対して**
  (`…/blob/573793b243fb1343636088eb62d1789ab2b14cec`)。`{{SOURCE_URL}}` 版を採点すると −3.1 pt ずれる。
- **`serve-ctl.sh` の罠 2 つ**: `grep -c` は 0 件でも "0" を出して exit 1 / サーバは対象リポジトリへ
  `cd` するので**渡すパスは全部絶対**。相対だと静かに死んで 600 秒待つ。
- **subagent には「コミットするな」と指示する** — 統合とコミットはオーケストレータが持つ。

## この晩に入れた訂正 (自分の書いたものを含む)

1. **§6.2 の未実装リストは欠落 36.4 pt のうち 3.0 pt しか説明していなかった** (最大項は
   リストに無い `splitWhitespaces` 32.9 pt)。検索ページ・静的資産は**母数の外で 0 pt**。
2. **「Linux では先読みは成立しない」は速いランナー実体でしか成り立たない主張だった** →
   実体で割れる、に訂正。**cold のレンジも 63〜71 秒 → 20〜89 秒**に是正。
3. **「集合を絞れば効く」を検証したつもりの実験に欠陥があった** — 集合を「import 後に
   *残っていた*ファイル」から作ったため必要分を取りこぼしていた (fault 2 倍)。
   **`oracle` ではなく `residual`。仮説は未検証のまま。**
4. **§6.6 の「CommonMark で +0.36 秒」は向きごと外れていた** — 実測は docstring 項が **−10.7%**。
5. **段階 7b の目標値 +0.613 pt が二重計上だった** (members 領域に紛れた autolink 分)。
6. **「意味解析の 75% は本質的コスト」は 1 スレッドの話だった** — 並列時は 51.3%。
