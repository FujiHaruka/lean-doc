# The micro fixture

This paragraph is `litedoc4.toml`'s `index`, rendered by the same Markdown
renderer a docstring goes through — so a code span like `Micro.double` looks the
way it does everywhere else.

* it is Markdown, not HTML
* it sits between the lede and the counts
* `tools/config-gate.sh` compares it across every command that writes this page
