import «Dep-Aux».Basic

/-!
# Dep-Aux

The root module of the path dependency. It exists so that the library has the
one shape `module_roots` looks for — a top-level `.lean` file — and so that the
name needing guillemets appears at the root rather than only under it.
-/
